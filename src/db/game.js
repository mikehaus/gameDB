class Game {
  constructor(uid, status, title, background_image, rawg_id, metacritic) {
    this.uid = uid;
    this.status = status;
    this.title = title;
    this.background_image = background_image;
    this.rawg_id = rawg_id;
    this.metacritic = metacritic;
  }
}
